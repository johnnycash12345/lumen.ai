# Site do Lumen

This is a code bundle for Site do Lumen. The original project is available at https://www.figma.com/design/yXQ38DrmHu5lOsxBK0RYKK/Site-do-Lumen.

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.